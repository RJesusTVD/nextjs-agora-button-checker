"use client";
import { SkipNavigation } from "@ama-pt/agora-design-system";

export default function SkipNavigationWrapper() {
  return (
    <SkipNavigation aria-label="Saltar para o conteúdo principal da página">
      Saltar para o conteúdo principal da página
    </SkipNavigation>
  );
}
